# Scripts

Repository maintenance scripts that are safe to version and reuse.

Temporary experiments, one-off diagnostics, and scratch scripts should not be committed; `.gitignore` excludes `_*.py` for that purpose.
