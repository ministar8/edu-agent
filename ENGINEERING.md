# EDU-Agent 工程指导报告

> 面向团队的技术评估与提升指南
> 评估方式：全量代码通读 + 工具实测（ruff / pyrefly / pytest --cov）+ 架构与测试结构分析
> 评估基线：`main` 工作区当前状态

---

## 0. 结论先行

**这是一份工程质量中上、架构清晰的项目，不是一份需要推倒重来的代码。**

真正的问题不是"代码写得烂"，而是**质量投入的分布与业务风险完全错配**：

> 系统 90% 的产品价值来自 RAG 检索链，但检索链是全项目测试覆盖最低、类型检查最松、函数粒度最大的部分。

因此本报告的核心结论是：**不要全面重构，而是把工程资源从"已经很稳的地方"搬到"风险最高但最薄弱的地方"。**

### 基线数据（实测，非估算）

下表的「初始」是本报告开工前的快照，「本轮后」是完成 P0 第一步/第三步/第四步之后复测的结果。
两列并列，是为了让后续读者能看到**改动到底换来了什么**，而不是只看一个孤立的现状数字。

| 指标 | 初始实测值 | 本轮后 | 评价 |
|---|---|---|---|
| 源码规模 | 19,188 行 / 102 个 `.py` | 19,968 行 / 104 个 `.py` | 中等，单人可通读 |
| 测试规模 | 5,910 行 / 47 文件 / **486 用例全绿** | 9,485 行 / 55 文件 / **937 用例全绿**（+3 skip） | 数量可观 |
| 整体覆盖率 | **36%**（8323 语句，漏 5358） | **52%**（8683 语句，漏 4182） | 上升，但**分布仍失衡** |
| `retriever.py` 覆盖率 | **7%**（747 语句） | **25%**（553 语句，删掉 194 行重复逻辑） | 上升 18pp，**靠"删代码"而非"写测试"** |
| `ruff check` | **0 告警** | 0 告警 | 优秀 |
| `ruff format --check` | 通过 | 通过 | 优秀 |
| `pyrefly` | **1 个真实类型错误** + 4 个 eval 可选依赖缺失（预期） | **0 个真实错误** + 5 个缺失（预期） | 良好 |
| 注释质量 | 899 行注释，大量解释"为什么" | 不变 | **显著优于同类项目** |
| CI 门禁 | lint + format + type + test + cov + docker build + md lint | **+ 检索质量黄金集门禁** | 覆盖完整 |
| **评测封闭性** | 无（依赖真实 TEI + 真实 LLM） | **无外部依赖**（假 embedding + 假 LLM + 临时索引） | **本轮修复的关键属性** |

> **读表提醒（本轮新增一行 `retriever.py` 就是为此）**：整体覆盖率 36% → 52% 里，
> 有一部分**不是靠写测试，而是靠删代码**换来的 —— 同步 `retrieve_documents` 那 447 行
> 重复流水线删除后，`retriever.py` 的分母从 747 降到 553，覆盖率自然从 18% 升到 25%。
> 这正是 §1 P0 第一步说的"结构性上限"：**在 428 行的单体函数被拆开之前，
> 覆盖率的分子很难再涨，但分母可以靠消除重复来缩小。**

### 已经做对的、必须保留的东西

这些是团队的**真实资产**，后续改进不能破坏它们：

1. **分层清晰且有文档**：`agents / rag / core / service / memory / schema` 职责边界明确，`CLAUDE.md` 把"注册新 agent 要同步三处""配置一律走 settings 单例"这类**约定显式化**了。多数团队做不到。
2. **设计决策留痕**：`supervisor.py` 里"为什么必须用 `Command.PARENT`""为什么四条循环出口"、`settings.py` 里"为什么删掉 8 个未引用超时字段"、`core/llm.py` 里"假模型为什么不能进缓存"——这些注释价值极高，直接降低了后来者的踩坑成本。
3. **防御性设计到位**：专家输出未闭环时 fail-fast（而非静默递归）、`AGENT_RECURSION_LIMIT` 显式收紧（避免默认 10007 步的费用事故）、thread 归属校验防越权读会话、密钥统一 `SecretStr`、缓存全部有界（`BoundedCache` + TTL + `on_evict`）。
4. **装配期校验**：`_assert_hook_covers_specialists` 把"改名漏配导致静默失效"提前成启动期崩溃。这是资深工程师才会写的代码。

**判断**：团队里已经有能写出高质量代码的人。当前问题更多是**质量纪律没有覆盖到最复杂的模块**，而不是能力全面缺失。

---

## 1. 质量问题与技术债务（按优先级）

### P0 — RAG 检索链处于"测试真空"

**证据**（来自 `pytest --cov` 实测）：

| 模块 | 行数 | 覆盖率 | 说明 |
|---|---|---|---|
| `src/rag/retriever.py` | **1872** | **7%** | 检索总门面，核心链路 |
| `src/rag/splitter.py` | 1181 | **0%** | 文档切分，决定 chunk 质量 |
| `src/rag/semantic_cache.py` | 661 | **0%** | 语义缓存，命中即返回 |
| `src/rag/vectorstore.py` | 500 | **0%** | 向量库读写 |
| `src/rag/embeddings.py` | 373 | **0%** | Embedding 客户端 |
| `src/rag/ingest.py` | 189 | **0%** | 入库流水线 |
| `src/rag/recall.py` | 534 | **16%** | 多路召回路由 |
| `src/rag/verifier.py` | 595 | **20%** | 证据校验 |
| `src/rag/fusion.py` | 346 | **21%** | 证据融合 |
| `src/rag/reranker.py` | 166 | **26%** | 重排 |
| `src/rag/postprocess.py` | 562 | **39%** | RRF / 窗口展开 |

对照组（说明不是"测不动"，而是"没去测"）：

| 模块 | 覆盖率 |
|---|---|
| `src/service/service.py` | 82% |
| `src/agents/tools.py` | 91% |
| `src/core/settings.py` | 96% |
| `src/memory/window.py` | 90% |

**风险**（为什么这是 P0，而不是"慢慢补"）：

1. **改动无法验证**。`retriever.py` 里有 `SCORE_THRESHOLD = 0.06`、`_RERANK_EXPAND_FACTOR = 5`、按查询类型 1.2/0.75/1.1 的阈值微调、按类型 5/6 的候选池倍率——注释写着"基于采样校准"。**今天没有任何自动化手段能证明改动这些常数不会让检索质量退化**。每次调参都是盲调 + 人工抽查。
2. **重构失去安全网**。想拆那个 447 行的函数，但没有测试证明"拆完行为不变"，于是没人敢动。技术债由此固化。
3. **失败模式被掩盖**。检索出问题时的表现是"答得不对"，而这条链上任何一环静默降级（rerank 超时、缓存污染、路由全部 miss）都只是"答得不对"，无法定位。
4. **`codecov.yml` 只排除了 `src/tools/`**，导致 36% 的总覆盖率**看起来只是"中等"**，掩盖了风险高度集中这一事实。指标失去了指导意义。

**改进方案（可落地，分三步）**：

- **第一步（1 周内，低成本高收益）**：把纯函数抽出来单测。`_apply_rerank_threshold`、`_resolve_retrieval_policy`、`_route_adaptive_k`、`weighted_rrf_merge`、`dedup_same_section`、`_route_timeout_seconds` 都是**无外部依赖的纯逻辑**，不需要 TEI、不需要 ChromaDB，直接喂输入断言输出即可。

  > **✅ 已落地（本轮）**：新增 `tests/rag/test_retriever_pure.py`，47 个用例覆盖上述决策逻辑，
  > 全部经反向验证（注入 9 类真实缺陷 → 断言全部变红 → 恢复后回绿）。
  > 实测 `retriever.py` 覆盖率 **7% → 18%**，`recall.py` 16% → 20%，全量 555 passed。
  >
  > **注意：实测值 18% 低于本节原先预估的「30%+」，这个差距本身是最重要的信息。**
  > `retriever.py` 的 747 条语句里，绝大多数位于两个 400+ 行的流水线函数中，而它们必须
  > 跑完整链路（依赖 TEI / ChromaDB / LLM）才可能被执行 —— 纯函数测试能触达的语句有**结构性上限**。
  > 也就是说：**不拆函数，覆盖率就上不去**；P0 的「补测试」与 P1 的「拆巨型函数」是同一件事的两面，
  > 必须并行推进，不能指望先补测试再重构。原先的乐观预估正是「以为测试可以绕过结构问题」的典型误判。
- **第二步（2-3 周）**：建立**检索质量黄金集回归测试**。`evals/` 已有 RAGAS 样本，把它从"离线手动跑的评测"升级为"CI 里跑的回归门禁"：固定 30-50 条 query + 期望命中的 `chunk_id`/`source`，断言 `recall@k`、`MRR` 不低于基线。**这是唯一能防止检索质量退化的机制**，优先级高于任何代码重构。

  > **✅ 已落地（本轮）**：新增 `src/evaluation/retrieval_gate.py` + `tests/evaluation/test_retrieval_gate.py`
  > （42 用例）+ `evals/retrieval_baseline.json`，并新增 CI job `retrieval-quality-gate`。
  >
  > **实测基线**（40 条 query / 2,205 chunk 索引 / 索引 22.5s + 检索 10.8s ≈ 33s，
  > **无任何外部依赖**：假 embedding + 假 LLM + 临时索引）：
  >
  > | 指标 | 基线值 | 说明 |
  > |---|---|---|
  > | `category_hit_at_1` | **0.900** | 首条命中属于目标学科 |
  > | `category_hit_at_k` | **1.000** | 至少一条命中目标学科 |
  > | `category_mrr` | **0.946** | 首个正确学科命中的平均倒数排名 |
  > | `category_precision` | **0.9005** | 返回证据中属于目标学科的比例 |
  > | `empty_result_rate` | **0.000** | 完全没返回证据的比例 |
  > | `mean_evidence_count` | **5.275** | 平均返回条数 |
  >
  > ⚠️ **基线必须在封闭环境下录**（本轮踩过坑）：早先的 0.9014 / 5.325 是**在真实 LLM 上**
  > 录出来的（`USE_FAKE_MODEL=true` 当时不生效，见 §1 P1），CI 用假模型对不上。
  > 录基线前先确认 `USE_FAKE_MODEL=true` 下 `get_llm()` 返回 `FakeToolModel`。
  >
  > **三处关键设计取舍**（都在模块 docstring 里写明了理由）：
  >
  > 1. **指标锚定「学科类目」而非「chunk_id」**。类目来自数据集自身的 `subject` 标签 ——
  >    客观 ground truth，不需要人工维护 40 条期望；而 chunk 级期望在任何一次切分或
  >    embedding 变更后都会大面积失效，维护成本远高于收益。原计划的"期望命中 chunk_id"
  >    被这一条替换了，理由是**可维护性**。
  > 2. **不依赖 TEI**：用确定性哈希 embedding + 临时索引，CI 无需任何外部服务。
  >    代价是它衡量的是**检索管线**（路由/融合/阈值/去重/窗口展开）是否退化，
  >    **不是语义质量**。因此基线文件里记录了 `_meta.embedding`，跨实现比较数字没有意义。
  > 3. **`empty_result_rate` 与 `mean_evidence_count` 必须与精确率一起看**：只看
  >    `category_precision` 有致命盲区 —— 返回 0 条时精确率无定义（易被算成完美）。
  >    这两个指标专治"静默返回空"这种最危险的退化形态。
  >
  > **反向验证 11/11**：注入的缺陷包括"精确率分母为 0 时返回 1.0"（把全空算成完美）、
  > "hit@1 复用 hit@k"、"忽略容差"、"删掉条数一致性校验"等，全部被断言抓到。
  > 另有 5 条**元测试**专堵"门禁假绿"（路径写错 → 0 条查询 → 永远通过）。
  >
  > **本轮暴露并处理的两个问题**（详见 §1 P1 与 P2）：
  >
  > 1. **门禁最初不可复现**：连续 4 次运行 `category_hit@1` 在 0.900~0.925 间抖动 ——
  >    噪声幅度（0.025）**大于容差（0.02）**，等于门禁无法区分"真实退化"与"随机抖动"。
  >    根因是 RRF 并列排序依赖线程完成顺序（§1 P1），已修复。
  >    修复 + 门禁侧自动重建后，连续 4 次运行指标**逐位一致**。
  > 2. **Chroma HNSW 段文件间歇性不落盘**（§1 P2）：会让某学科整轮检索返回错误结果
  >    却不报错，把门禁污染成"随机失败"。已落地检测 + 重建缓解；上游根因未定位。
  >
  > **这两条都比"检索质量退化"更有价值** —— 门禁上线第一天抓到的不是它要防的东西，
  > 而是两个此前无人知道的基础设施缺陷。
  >
  > **已知弱点（明确记录，不掩盖）**：`category_hit_at_1` 只有 0.900，4 条 query 首条命中
  > 落在别的学科，且都是**词面歧义**导致，与哈希 embedding 只有词汇重叠信号直接相关：
  >
  > | query | 期望 | 实际命中 | 歧义词 |
  > |---|---|---|---|
  > | 什么是段页式存储管理？ | os | computer_organization | 「存储」 |
  > | IP 地址与 MAC 地址有什么区别？ | network | operating_system | 「地址」 |
  > | 路由选择协议 RIP 和 OSPF 有什么不同？ | network | data_structure | 「路由」 |
  > | 指令周期包括哪些阶段？ | co | operating_system | 「指令」 |
  >
  > 换成真实 TEI embedding 后这 4 条大概率会改善，所以**这个 0.900 不是生产质量的估计值**，
  > 而是"同一环境下不许更差"的回归锚点。要测真实语义质量，仍须用 `evals/cli.py` 的 RAGAS。

- **第三步**：用 fake embedding（定长随机向量或 hash 向量）替换 TEI 依赖，让 `vectorstore / semantic_cache / recall` 能在 CI 里跑通。`pyproject.toml` 里已有 `USE_FAKE_MODEL` 的先例，照此模式加 `USE_FAKE_EMBEDDING` 即可，与现有测试哲学一致。

  > **✅ 已落地（本轮）**：新增 `settings.USE_FAKE_EMBEDDING` + `rag.embeddings.HashingEmbeddings`
  > （字符 bigram 哈希 + L2 归一化；用 `hashlib` 而非内置 `hash()` 以保证跨进程确定性，
  > 否则重启后同一文本得到不同向量、表现为"刚写入就查不到"），并在 `[tool.pytest_env]` 默认开启。
  > 新增 `tests/rag/test_rag_storage_integration.py`（30 用例）覆盖语义缓存的存取 / 归属校验 /
  > TTL / 容量淘汰 / 持久化 / 异步路径，以及向量库往返与集合隔离。
  >
  > **实测覆盖率（`coverage run` 全量）**：
  >
  > | 模块 | 改前 | 改后 |
  > |---|---|---|
  > | `rag/semantic_cache.py` | 0% | **57%** |
  > | `rag/vectorstore.py` | 0% | **35%** |
  > | `rag/embeddings.py` | 0% | **30%** |
  > | 全项目 | 36% | **41%** |
  >
  > 全量 **584 passed / 1 skipped**；7 类注入缺陷全部被断言抓到（含 `hashlib → hash()` 替换）。
  >
  > **同时暴露两个此前无人知道的真实风险**（因为这两条路径从未被执行过）：
  >
  > 1. `semantic_cache._DATA_DIR` / `_JSONL_FILE` 是**模块级常量、import 期即固化为真实项目路径**。
  >    测试若不显式重定向就会写坏真实 `chroma_db/semantic_cache/evidence.jsonl`。这是"配置在
  >    import 期捕获"的又一处实例（与 §1 P2「配置存在但不生效」同类），已在 fixture 中强制隔离
  >    并写入 CLAUDE.md 作为约定。
  > 2. **语义缓存是静默错误的高危区**：命中即跳过整条检索链，一旦归属校验或 TTL 判断出错，
  >    系统会把别的知识库集合、或已过期失效的结果当作正确证据返回，**且不报错**。
  >    这类路径此前 0% 覆盖，现已纳入回归网。
  >
  > 附带发现（环境层面，非项目缺陷）：本机沙箱的删除守卫会拦截 bulk delete，导致
  > ① `pytest --basetemp=<已存在目录>` 因清空该目录而失败；② pytest-cov 收尾 `combine()`
  > 删除并行数据文件时抛 `SystemExit`。两者都表现为"大批 error"，但 `pytest` 自身全绿 ——
  > 排查时**不要把它们误判为代码回归**。规避：`--basetemp` 每次用全新目录，改用
  > `coverage run -m pytest` 绕开 pytest-cov 的 combine。
- **第四步**：`splitter.py` 是"0% 覆盖"这批里最后一个，也是**投入产出比最高**的一个：
  它 1181 行里有 25 个纯函数，完全不需要外部依赖即可测试。

  > **✅ 已落地（本轮）**：新增 `tests/rag/test_splitter.py`（**218 用例**）覆盖全部纯函数
  > （阈值表 / 句界判定 / 语义单元解析 / 层级元数据 / 父子窗口）与 `split_documents` 端到端契约。
  > 实测 **`rag/splitter.py` 0% → 99%**（601 语句仅剩 6 行未覆盖，且均为不可达/纯防御分支），
  > **全项目 36% → 48%**；全量 **802 passed / 1 skipped**。
  > 反向验证 **18/18** 类注入缺陷全部被断言抓到（含"让 `chunk_size` 参数真正生效"这类
  > 反转缺陷 —— 专门用于确认"参数被忽略"这条哨兵断言有鉴别力）。
  >
  > **但这一轮真正的收获不是覆盖率，而是在真实语料上实测出一个此前无人知道的重大缺陷**：
  > 见下节 §1 P1「Q&A 原子机制在真实语料上从未激活」。

---

### P1 — `splitter` 的 Q&A 原子机制在真实语料上从未激活（死代码 + 死检索路由）

**背景**：补 `splitter.py` 测试时，顺手用真实知识库做了端到端实测，结果发现文档承诺的
「Q&A 对合并为原子单元、不可拆分」这条核心特性，**在 40 个真实知识文件上一次都没生效**。

**实测基线**（`split_documents` 跑全库 `knowledge/**/*.md`，共 40 个文件）：

| 指标 | 实测值 |
|---|---|
| 输入字符 / 产出 chunk | 755,509 / 2,217 |
| 输出字符 / 字符留存率 | 731,740 / **96.9%** |
| 平均 chunk 长度 | 330 字符 |
| `merged_qa` 角色 chunk 数 | **0** |
| `content_type` 实际取值集合 | 仅 `{code_mixed, section}` |

**根因**：`_ANSWER_RE = (?:解答|答案|正确答案)[：:；;]` 与真实语料的标注方式不匹配。

| 真实语料中的标注 | 出现次数 | 是否被 `_ANSWER_RE` 识别 |
|---|---|---|
| `**解析**：` | 240 | ❌ |
| `✅`（标在正确选项行尾） | 251 | ❌ |
| `答案：` | 66 | ✅ |
| `解答：` | 26 | ✅ |
| `正确答案：` | **0** | ✅（但从不出现） |

408 真题用的是「选项行尾打 `✅` + `**解析**：`」，所以 `has_answer_marker` 恒为 False →
`content_type` 永远不会被提升为 `merged_qa` → `_extract_qa_fields` / `_split_qa_oversized` /
`qa.*` 元数据 / `section.chunk_role="merged_qa"` **整条链路从未执行过**。

**连带死代码（跨 4 个模块，这是最值得警惕的部分）**：

| 位置 | 内容 | 实际状态 |
|---|---|---|
| `recall.py:216-231` | `merged_qa_meta` 专用召回路由（按 `content_type="merged_qa"` 过滤） | **恒返回空** |
| `recall.py:283-285` | RRF 权重 `(merged_qa_meta, exercise/answer/comparison)=2.0/2.0/1.5` | 永不生效 |
| `_metadata_spec.py:67-70` | `qa.question` / `qa.answer` / `qa.answer_key` 元数据契约 | 契约有、索引里没有 |
| `rag_utils.detect_content_type:163-166` | `merged_qa` 分支 | 永不触发 |
| `splitter.py` | `_merge_qa_blockquotes` / `_extract_qa_fields` / `_split_qa_oversized` | 约 300 行未被执行 |

**为什么比"功能缺失"更危险**：这是**声明与实现不一致**。下游若按 `qa.answer_key` 取值，
拿到的永远是空串；`recall` 那条路由会白白消耗一次向量检索配额并恒空返回。
关键在于 —— **没有任何日志或指标会提示它没生效**。同一类病在 §1 P2「配置存在但不生效」
已经出现过一次，这次是它的"数据契约版"。

**修复方向（需产品决策，不建议直接改）**：

1. **对齐标注**：扩展 `_ANSWER_RE` 使其识别 `✅` 与 `**解析**：`；或反向统一知识库格式
   （后者要改 6 个真题文件共 278 处）。
2. **先修 `_merge_qa_blockquotes` 的结构性缺陷**（见下表 #2/#3），否则对齐了标注也仍然拆不开。
3. **决定 `merged_qa` 路由的去留**：要么让它有数据，要么删掉路由与权重，别让它"看起来能用"。

> ⚠️ 改动会改变 chunk 切分结果 → 必须重新入库 → 检索质量可能变化。
> **应先用 P0 第二步的「黄金集回归测试」兜底，再动手。**

**本轮用测试钉住的具体缺陷**（每条都在 `tests/rag/test_splitter.py::TestKnownDefects`
留了变更哨兵：断言的是现状，修复后必然变红并迫使同步更新契约）：

| # | 缺陷 | 影响 |
|---|---|---|
| 1 | `split_documents(docs, chunk_size, chunk_overlap)` 的**两个参数在函数体内从未被引用** | 调用方任何调参都静默失效；实际尺寸只由 `_ADAPTIVE_CHUNK_SIZE` 决定 |
| 2 | `_merge_qa_blockquotes` 的输出与 `_parse_semantic_units` 的空行切分**不兼容** | `__QA_PAIR_START__` 只贴在首行，块内空行使原子 Q&A 被 `re.split(r"\n{2,}")` 切碎，答案与题干分离 |
| 3 | 例题模式遇到「答案前的空行」即提前关闭 | 这正是 markdown 常态写法，等于该模式基本不可用 |
| 4 | 短例题 section **产出 0 个 chunk** | 内容静默丢失、不进索引（合成用例：`## 例题精讲` + 短例题 → 0 chunk）。**真实语料上影响很小**，见下方「真实留存率实测」 |
| 5 | 原子单元（表格/公式）会被 `_merge_short_chunks` **合并回邻居** | ⚠️ **经门禁实测后判定为「有意为之，不是缺陷」**，详见下方「一次被门禁拦下的错误修复」 |
| 6 | `_split_oversized` 注释声称有「单句超 limit 的兜底」，**实际不存在** | ✅ **已修**：500 字符单句在 limit=100 下仍产出 1 个 500 字符 chunk（现按 limit 硬切，反向验证 3/3） |
| 7 | `_extract_headings` 只认 `#{1,4}` | 真实库中 **80 处** `##### 1)内容` 五级标题完全不可见，section 层级与 `heading_path` 失真 |
| 8 | `_add_prose_unit` 的 1200 上限**未计入拼接空格** | 实测产出可达 1319 字符，超出自设上限 |
| 9 | 代码块占位符自带换行（`\n__CODE_BLOCK_n__\n`） | 在 Q&A 块内制造空行，同样触发切碎 |
| 10 | `content_type in ("exercise","answer")` 是**死条件** | 调用 `detect_content_type` 时未传 `source_ext`，这两个分支永不命中 |
| 11 | 不可达代码：`_is_sentence_complete` 的 `endswith(">")` 分支、`_split_into_sections` 的 `not section_text`、`_build_parent_window_text` 的 `total >= budget` 分支 | 冗余/失效守卫，掩盖真实控制流（也是 99% 覆盖后剩下那 6 行） |
| 12 | 前导文段占用 `section_index=0` | 首个真实 section 从 1 开始，索引 0 语义歧义 |

**真实留存率实测（修正一处我此前的过度表述）**

我在上一轮把「短 section 产出 0 chunk」描述为**严重内容丢失**。在真实语料上量了一遍，
结论需要修正：

| 指标 | 实测值 |
|---|---|
| section 总数 | 2,005 |
| 产出 0 chunk 的 section | 362（18%） |
| 输入字符 / 实际落库字符 | 753,776 / 735,734 |
| **留存率** | **97.6%** |

关键在于 **362 个"丢失" section 里有 318 个是「只有标题、没有正文」的空 section**
（如 `4.1 常见的计算机网络体系结构`，全文 20 字符就是标题本身）。这类 section 本就没有
可检索的内容，被过滤掉是**正确行为**，不是缺陷。

真正的丢失来自 `_flush_chunk` 把不完整的尾部摘出来后，两部分都低于 `MIN_CHUNK_LENGTH`
被一起丢弃 —— 量级约 2.4%，属于"可以更好，但不紧急"。

> **教训**：我在上一轮用**合成用例**（`## 例题精讲` + 短例题 → 0 chunk）推出"严重丢失"，
> 没有先量真实分布。合成用例能证明"存在这条路径"，但证明不了"这条路径有多重要"。
> **缺陷的严重性必须用真实数据量，不能用手写的极端样本推。** 这与 §3.1-G 的
> "极端结论先复现"是同一条纪律的另一面。

**一次被门禁拦下的错误修复（本轮最有价值的过程记录）**

上表第 5 条我原本判定为缺陷：`_merge_short_chunks` 会把过短的表格/公式 chunk 合并回邻居，
违反 `_split_section_text` docstring 里"不可与其它单元合并"的承诺。我据此加了 `is_atomic`
标记与守卫，测试也写好了（断言"三个短单元不应被合并"），逻辑上完全站得住。

**然后门禁把它拦下来了**：

| 指标 | 修复前 | 修复后 |
|---|---|---|
| 索引 chunk 数 | 2,205 | **2,166**（少 39） |
| 全库字符留存率 | **97.6%** | **96.5%** |
| `category_precision` | **0.9014** | **0.8952** |
| `mean_evidence_count` | **5.325** | **5.250** |

原因：`_merge_short_chunks` 的合并是**整体拼接**，公式/表格的内部结构始终完整 ——
它根本没有"拆开"任何东西。而禁止合并之后，过短的原子 chunk 会**连同它过短的邻居一起**
落到 `MIN_CHUNK_LENGTH` 之下被丢弃。

所以 docstring 里"不可与其它单元合并"这句话本身就是错的：**原子性在这里的准确含义是
"不被拆分"，而不是"不参与合并"**。已回退代码，改为修正 docstring，并把"允许合并"
这个决定用测试钉住（附上这组实测数字），防止后人再"修"一次。

> **这是门禁上线后第一次拦下一个真实的质量退化，而它拦下的不是别人写的代码 —— 是我自己
> 一次逻辑自洽、测试完备的"正确修复"。** 单测证明不了端到端质量，这一条就是最直接的证据：
> 那三个新测试全绿，代码审查也挑不出问题，唯一能发现它的是端到端指标。

**这一节的元教训**：覆盖率从 0% 拉到 99%，**价值不在数字本身，而在于"第一次真正执行了这些路径"**。
上述 12 条里有 9 条只有在真实数据上跑起来才会暴露 —— 这也反向印证了 §1 P0 的判断：
**测试真空的真正代价不是"没测试"，而是"没人知道代码实际在做什么"**。

---

### P1 — 两个"双胞胎巨型函数"是检索链的真正病灶

**证据**：

| 函数 | 初始行数 | 本轮后 | 位置 |
|---|---|---|---|
| `retrieve_documents`（同步） | **447** | **45**（薄委托） | `retriever.py:681` |
| `aretrieve_documents`（异步） | **427** | 428 | `retriever.py:725` |
| `aretrieve_evidence` | 143 | 143 | `retriever.py:1155` |

两份逻辑近乎平行复制。文档说同步版"**仅供 ingest 后的缓存预热**"，但它承载了 447 行完整流水线逻辑。

**为什么这是问题**：

- **同步/异步双份实现必然漂移**。改了一侧忘了另一侧，就是"预热出来的缓存和真实检索结果不一致"这类极难排查的 bug。
- **单函数 447 行意味着不可测**。圈复杂度极高，分支组合爆炸，无法为单条分支写测试——这直接**解释了** P0 的 7% 覆盖率。二者是同一个病根。
- **不可复用**。`retrieve_documents` 里的阶段（归一化 → 召回 → 融合 → 校验 → 展开）无法被单独调用或替换。

**改进方案**：

把巨型函数按**已存在的阶段边界**切成语义化阶段函数（注意：`retriever.py` 的 docstring 已经写清了这些阶段，切分是"把注释变成函数"，不是重新设计）：

```
_normalize_and_classify(query) -> (norm_query, cat, terms)
_resolve_policy_and_k(...)      -> (threshold, coarse_k)      # 已有 _resolve_retrieval_policy
_recall_multi_route(...)        -> route_results              # 已有 _multi_route_search
_merge_and_dedup(...)           -> merged                     # 已有 weighted_rrf_merge
_rerank_and_filter(...)         -> reranked                   # 已有 rerank + _apply_rerank_threshold
_expand_and_fuse(...)           -> fused                      # 已有 sentence_window_expand + fuse_evidence
```

切完后 `retrieve_documents` 应该收敛成**约 30-50 行的编排函数**。同时：

- 同步版**只保留一个**：让同步路径直接 `asyncio.run(异步版)`，或反过来让异步版成为唯一实现。**消除双份逻辑比拆函数更重要**。

  > **✅ 已落地（本轮）**：先做了"更重要的那一半"。同步 `retrieve_documents`
  > **447 行 → 45 行薄委托**（`asyncio.run(aretrieve_documents(...))`），
  > `retriever.py` 单文件 **1873 → 1470 行**（净删 403 行）。
  >
  > **动手前先量了漂移**（而不是假设它已经漂移）：15 条真实 query 上
  > 条数 / 集合 / 顺序**全部一致** —— 也就是说**双份实现当前还没漂移，风险是潜在的而非已发生的**。
  > 这一点值得写下来：报告原文说"必然漂移"，实测是"尚未漂移"。**推断与实测必须分开陈述。**
  >
  > **为什么仍然要改**：它唯一的调用方是 `warmup_query_cache`（入库后缓存预热），
  > 为一个场景维护 447 行重复逻辑不划算；而且这 447 行是 `retriever.py` 覆盖率
  > 上不去的直接原因之一。
  >
  > **正确性依据（两条，缺一不可）**：
  > 1. **端到端等价性**：真实索引上 12 条 query，同步与异步返回的文档序列逐条相同；
  > 2. **检索质量门禁**：六项指标与基线**完全一致**，exit=0。
  >
  > 另外新增 9 条结构性守卫（`tests/rag/test_retriever_sync_bridge.py`）：签名必须与异步版一致、
  > 返回值必须原样透传、9 个形参必须全部转发、同步版不许再出现流水线细节
  > （`_multi_route_search` / `weighted_rrf_merge` / `stage_ms` 等字样出现即失败）、
  > 在运行中的事件循环里调用必须给出可读错误。**反向验证 4/4**。
  > 最后一条守卫是专门防"后人顺手又把流水线抄回去"的。
  >
  > **仍未做**：`aretrieve_documents` 本身还是 428 行的单体函数。上表那套阶段函数拆分
  > 是它的下一站 —— 那才是真正让覆盖率能上去的动作（见 P0 第一步的"结构性上限"）。
  > 本轮先消除重复，是因为**改动面小、可用端到端指标证明无损**；拆 428 行的流水线
  > 需要先有阶段级的行为基线，否则是在没有安全网的情况下重构。

- 每个阶段函数都有明确输入输出 → 立即可以单测 → 与 P0 第一步形成合力。

**建议目标（写进规范）**：单函数 ≤ 60 行；单文件 ≤ 600 行；超出必须拆分或说明理由。

---

### P1 — 宽泛异常处理吞噬错误，调试成本高

**证据**：全项目 **92 处 `except Exception`**，其中 **13 处 `except: pass`**（`semantic_cache.py:154/215/524/607`、`loader.py:43/67`、`parse_utils.py:57/65/73`、`evidence.py:80`、`imputer.py:721`、`ragas_eval.py:100/108`）。

**典型模式**：

```python
except Exception as e:
    logger.error("Retrieval failed: %s", e, exc_info=True)
    return RetrievalResult(status="error", context=f"检索失败：{e}", ...)
```

**为什么是问题**：

- 调用方拿到的是**字符串化的错误**，无法区分"知识库确实没有"（`status=empty`）、"外部服务挂了"（TEI 超时）、"代码写错了"（`KeyError`）。**这三者需要完全不同的处置，现在被抹平成同一类**。
- `except: pass` 更严重：`semantic_cache.py` 静默吞掉异常意味着"缓存坏了但你以为在缓存"，性能与成本问题会长期潜伏。
- 92 处 catch-all 意味着**日志里出现 ERROR 时，你无法判断严重程度**——真实故障被淹没在噪声里。

**改进方案（分层错误策略，见 §2 规范）**：

1. **定义异常层级**，替代字符串化错误：
   ```python
   class RagError(Exception): ...              # 检索域基类
   class RetrievalUnavailable(RagError): ...   # 外部依赖不可用 → 可降级/可重试
   class RetrievalEmpty(RagError): ...         # 正常空结果 → 不是错误，不记 ERROR
   class RagInternalError(RagError): ...       # 代码缺陷 → 必须告警
   ```
2. **禁止 `except: pass`**：确需忽略时，必须 `logger.debug(..., exc_info=True)` 并写注释说明为何安全。
3. **catch-all 只允许出现在"最外层边界"**（HTTP 路由、agent 工具入口），且必须区分 `except (RetrievalUnavailable, TimeoutError)` 与 `except Exception` 两级，后者一律 `logger.exception` + 上报。
4. **空结果不是错误**：`status=empty` 走正常返回路径，不产生 ERROR 日志。

---

### P1 — 类型检查对"最复杂的模块"最宽松

**证据**（`pyproject.toml`）：

```toml
[[tool.pyrefly.sub-config]]
matches = "src/rag/**"
errors = { bad-argument-type = false, missing-attribute = false, not-iterable = false,
           bad-assignment = false, unbound-name = false, bad-return = false,
           not-callable = false, bad-unpacking = false, missing-import = false }
```

`src/tools/**` 同样关闭了 9 类检查。CLAUDE.md 自己标注为"技术债"。

**为什么是问题**：`unbound-name`、`not-callable`、`bad-unpacking` 被关掉的正是**最容易产生运行时崩溃**的几类错误。`rag/` 是全项目分支最多、数据结构最复杂的目录（`list[tuple[Document, float]]` 到处传递），恰恰最需要类型保护。**把安全带系在最稳的座位上**。

**改进方案（渐进式，不要求一次修完）**：

- 先**打开代价最低、收益最高的一类**：`unbound-name`（未绑定变量，纯 bug 探测器）和 `missing-import`（改名/删文件残留）。这两类修复成本低、几乎不会误报。
- 然后开 `bad-argument-type` + `bad-return`，用 `# type: ignore[code]` 精确豁免个别误报，**而不是整目录关闭**。
- **规范**：新增代码不得使用整目录放宽；存量放宽必须逐项记录在 CLAUDE.md，并在每次迭代顺手收敛。
- 顺手修掉当前唯一的真实类型错误：`supervisor.py:176` 用 `frozenset` 调 `make_forward_after_agent(set[str])`（改函数签名为 `AbstractSet[str]` 即可）。

---

### P1 — 检索结果不可复现：RRF 并列排序依赖线程完成顺序

**证据**（本轮建立质量门禁时暴露）：同一份代码、同一份索引、同一条 query，连续 4 次运行
门禁，指标在噪声区间内抖动：

| 运行 | `category_hit@1` | `category_precision` | `mean_evidence_count` |
|---|---|---|---|
| 1 | **0.9250** | **0.9151** | **5.3000** |
| 3 | 0.9000 | 0.9014 | 5.3250 |
| 4 | 0.9000 | 0.9014 | 5.3250 |

**根因**（`rag/postprocess.py`，两处同款）：

```python
ranked.sort(key=lambda item: item[1], reverse=True)   # merge_route_results / weighted_rrf_merge
```

Python 的 `sort` 是**稳定排序**：只按分数做键时，**分数并列的文档会沿用插入顺序**。
而插入顺序来自 `merged` 字典的写入次序 —— 也就是 `_multi_route_search` 里
`ThreadPoolExecutor` + `as_completed()` 的**线程完成次序**。于是：

> **同一条 query，每次运行返回的证据排列都可能不同。**

**为什么这是 P1，而不是"测试有点抖"**：

1. **产品行为不可复现**：同样的提问，两次可能拿到不同顺序的上下文 → LLM 答案不同。
   用户投诉"同一个问题答案不一样"时，这条链上没有任何日志能解释。
2. **门禁失效**：噪声幅度（`hit@1` 约 0.025）**大于**容差（0.02），
   意味着"1 条 query 的真实退化"与"随机抖动"无法区分 —— 门禁要么漏报、要么误报。
   这是"指标失去指导意义"的又一实例（同 §1 P0 第 4 条）。
3. **偶发 bug 会因此变成"不可复现"**：任何依赖结果顺序的下游逻辑
   （去重、截断、top-k 选择）都会继承这个不确定性。

**修法（已落地）**：给排序加**确定性的次级键**：

```python
ranked.sort(key=lambda item: (-item[1], _dedup_key(item[0])))
```

`_dedup_key` 就是 `merged` 的键，因此唯一且确定，能给出全序。两处排序都已修正。

> **反向验证 4/4**：退回稳定排序、次级键取反、主键方向写反、只改一处 —— 全部被断言抓到。
> 修完后门禁连续 4 次运行指标**逐位一致**。

**这里有一条更普适的教训**：**在拿一个门禁去判定别人之前，先测量门禁自身的噪声。**
本轮如果直接接受"跑三次都一样"的巧合，就会把一个噪声高于容差的门禁当成质量闸门，
后续所有"退化"判定都不可信。

---

### P1 — 假模型开关不封闭：`USE_FAKE_MODEL=true` 时检索链仍调用真实模型

**这是本轮最严重的发现，因为它同时污染了"测试"和"评测基线"两件事，而且全程不报错。**

**根因（两处叠加）**：

1. **`get_llm()` 没有 fake 分支。** `get_model()`（agent 层）有：
   ```python
   if parse_model_ref(model_ref)[0] is Gateway.FAKE:
       return FakeToolModel(...)
   ```
   而 `get_llm()`（RAG 检索链）直接走 `_cached_client` → 构造真实 `ChatOpenAI`。
2. **`USE_FAKE_MODEL` 不覆盖 `LLM_MODEL`。** `model_post_init` 只在
   `DEFAULT_MODEL` 为空时填假值，而 `LLM_MODEL` 的**默认值本身就是真实模型**
   （`deepseek:deepseek-v4-flash`），`.env` 里通常还显式配了真实引用。
   更糟的是 `_validate_model_ref` 在覆盖之前执行 —— 无 key 环境会在覆盖生效前就先抛错。

**后果分两种环境，都很难察觉**：

| 环境 | 表现 |
|---|---|
| 本地（`.env` 有真实 key） | 检索链**真的调用线上模型**。decompose / HyDE 用温度 0.3 → 每次结果不同、基线不可复现、产生费用。**没有任何报错。** |
| CI（无 key） | `openai.OpenAIError: Missing credentials`。实测 **40 条门禁 query 里 6 条直接崩溃**，异常穿透 `aretrieve_evidence_with_retry` 之外，门禁只留下 traceback。 |

**实测证据**：同一份代码跑门禁，本地（真实模型）与封闭环境（假模型）指标不同 ——

| 指标 | 真实模型（本地） | 假模型（封闭） |
|---|---|---|
| `category_precision` | 0.9014 | **0.9005** |
| `mean_evidence_count` | 5.325 | **5.275** |

**也就是说：此前提交的检索门禁基线，是"跑在真实 LLM 上"录出来的。** CI 用假模型，
数字对不上，门禁在 CI 里要么误报要么靠容差侥幸通过 —— 而我当时把连续 4 次一致
当成了"确定性已修复"的证据。**那是巧合，不是设计。**

**修法（三处，已落地）**：

1. `get_llm()` 补 fake 分支（与 `get_model` 对称，且 `FakeToolModel` 不进缓存 —— 它有状态）。
2. `model_post_init` 在**任何校验之前**把 `DEFAULT_MODEL` 与 `LLM_MODEL` 一起拉回假网关。
   顺序是关键：放在校验之后，无 key 环境会在覆盖生效前就抛错（这条是测试先抓出来的）。
3. `configure_for_gate` 显式设置两个模型引用（**故意的重复**：门禁的正确性不该依赖
   settings 的默认推导），CI job 也显式加 `USE_FAKE_MODEL=true`（否则 settings 构造阶段就失败）。
4. `run_gate` 改为**单条 query 抛异常不中断整轮**：记成"空结果 + 错误原因"，
   门禁照样给出完整指标并单独列出错误；`--update-baseline` 遇到异常直接拒绝录基线
   （否则会把故障固化成基准）。

**顺带修掉的同类问题**：`_validate_model_ref` 与假值覆盖的顺序（见上）。

**新增 15 条测试**（`tests/core/test_fake_model_hermeticity.py`）：断言落在
"拿到的客户端类型"和"模型引用"上，而不是"没抛错" —— 这类缺陷的共性是**默认路径看起来是好的**。

> **教训（已写进 §3.1-G）**：**"没报错"不等于"环境是封闭的"。**
> 评测类改动的验收标准必须包含"它依赖哪些外部资源"，并在**无外部资源的条件下**跑一遍。
> 本轮如果一开始就用"清空凭据"的方式验证门禁，这个缺陷会在第一次就暴露。

---

### P1 — recall 阶段残余非确定性：已定位到 Chroma ANN 边界（低频、被下游吸收）

**怎么发现的**：为拆分 `aretrieve_documents` 而新建的阶段级追踪器（`evaluation.stage_trace`）
记录每个阶段函数的入参与返回值。它比门禁**敏感得多**：门禁只看最终 top-k 的身份与聚合指标，
而追踪器能看到中间阶段。

**逐层定位（关键方法）**：把召回链按层 dump 签名，找**最低**发生差异的那一层：

| 层 | 内容 | 跨进程一致性 |
|---|---|---|
| L1 | `avgdl`（每个集合的平均文档长度） | **一致** |
| L2 | `bm25_search` 的 top-k | **一致** |
| L3 | `_raw_search` 逐路由的候选 | **不一致** ← 最低差异层 |
| L4 | `_amulti_route_search` 的完整输出 | 一致 |
| L5 | `aretrieve_documents` 的最终文档 | 一致 |

**具体表现**（`computer_organization:semantic`，"栈和队列的主要区别是什么？"）：

```
run1: … ('dbea6aaf', 0.8573), ('50296df0', 0.8609), ('e30b26eb', 0.8610)
run2: … ('84911dc4', 0.8501), ('dbea6aaf', 0.8573), ('50296df0', 0.8609)
       ↑ 距离更优（0.850 < 0.861），但 run1 没返回它
```

**这是近似检索的漏召**：不是"顺序不同"，而是**首次访问时少返回了一个本该进 top-k 的候选**。

> **⚠️ 更正（后续实测推翻了我先前的结论）**：我一度写"该差异被下游融合/去重吸收，
> 产品侧无可观测影响"。**这个结论是错的。** 在做拆分验证时捕获到
> `虚拟内存的作用是什么？` 的 **`最终返回的文档序列变了`** ——
> 即漂移**确实会传导到最终结果**。产品存在低频的"同一问题返回不同证据"，
> 频率约 10 次里 1 次。这对 #28 的影响是：**逐位比对最终结果会周期性误报**，
> 故新增 `probe_query_stability` + `--stable-only`，录基线时先剔除不可复现的 query。

**已排除的原因（全部为实测，不是推测）**：

| 假设 | 实测结论 |
|---|---|
| `decompose` 调真实 LLM 导致子查询漂移 | 成立但**已修**（见上一条 P1）；修完后仍有余量 |
| Chroma HNSW 跨进程重建导致 ANN 不同 | **排除** —— 复用同一持久化索引、且不重建，仍出现 |
| `bm25_search` 的 `avgdl` 采样抖动 | **排除** —— 清缓存连续 5 次重算，取值唯一 |
| `bm25_search` 本身不确定 | **排除** —— 隔离测试 top-k 签名唯一 |
| 原始 `coll.query`（固定向量）不确定 | **排除** —— 进程内 6/6 一致、跨进程 3/3 一致 |
| LangChain `Chroma` 包装器不确定 | **排除** —— 跨进程 3/3 一致 |
| RRF 并列排序依赖线程完成次序 | 已修，**不是**残余原因 |

**⚠️ 未能复现（这一条同样重要）**：初次观测到 run1 离群后（8 次里 1 次），
我用**全新索引**做了受控 A/B —— 不预热 3 次 + 预热 3 次，**6/6 全部一致**。
也就是说：**这是低频、间歇现象，不是系统性缺陷**，我无法稳定复现它。

**结论**：根因在 **Chroma 近似向量检索的 top-k 边界**（上游），不在本项目代码。
本项目的三个可改点（`bm25_search` 的无序截断 / 无序候选集 / 排序无次级键，见 backlog #31）
经隔离测试都是确定的，因此**不是本次现象的原因**，但仍是值得加固的隐患。

**对 #28（拆分 `aretrieve_documents`）的影响与解法**：
不能对 recall 阶段做**逐位严格比对** —— 那样会把上游 ANN 的边界抖动当成重构引入的退化。
正确做法是**分级断言**：

- 阶段调用**序列**与各阶段**入参**：严格一致；
- recall 层**输出**：**只报警、不判失败**（见下）；
- 融合及其之后的所有阶段：严格一致（这部分是我们自己的代码，必须逐位可比）；
- 最终返回：严格一致。

这样既守住了"重构不改行为"的真正契约，又不会因上游近似索引而假报警。

> **✅ 已落地（本轮）**：`evaluation.stage_trace.compare_traces` 实现分级比对，
> 严格模式下 6 次连跑全部通过（附 2 处"因上游抖动无法判定"的明示），反向验证 5/5。
>
> **调参过程本身是一条教训**：我先给 recall 层设了"容许 2 个文档差异"的**绝对阈值**，
> 实测漂移在 2~6 之间波动 → 误报；改成 15% **比例** → 仍偶发误报；改成 30% → 仍偶发。
> 最后才想明白：**对一个上游近似索引的输出设任何硬阈值都会周期性误报**。
> 改为"只报警不判失败"，并把安全网建立在**入参断言 + 最终返回断言**上 ——
> 这两处都是严格的，recall 真退化（如传错 k）依然会被抓到。
>
> **"无法判定"必须显式报出来**：如果分级比对只输出"通过"，会掩盖掉"其实有一大段没验证到"。
> 所以 `TraceComparison` 把 `advisories` 与 `failures` 分开，摘要里也带上条数。
>
> **补测发现的一个真实缺口**：反向验证时我构造了"`fed` 判定永远为真"的缺陷，
> 但原有测试没抓到它 —— 因为那些用例里 recall **没有**抖动（`volatile_drifted=False`），
> `fed` 的对错不影响结果。补了一条"recall 抖动 + 不吃 recall 输出的阶段也变了"的用例
> 才覆盖到。**这正是"缺陷注入要挑对触发条件"的价值**：测试全绿不等于覆盖到了关键路径。

---

### P2 — 魔数散落 + 路由名单硬编码，调参和扩展都危险

**证据**：

- 硬编码阈值分布：`retriever.py`（`SCORE_THRESHOLD=0.06`、`_RERANK_EXPAND_FACTOR=5`、`_CACHE_TTL=300`）、`fusion.py`（`_KG_TEXT_BOOST=1.2`、`_PARENT_WINDOW_BONUS=1.08`、`_HYDE_PENALTY=0.95`）、`postprocess.py`（`_RRF_K_BASE=20`、`_NOISE_MIN_COVERAGE=0.15`）、`reranker.py`、`bm25.py` 等数十个。
- `_resolve_retrieval_policy` 里**硬编码了 14 个路由名**的列表，而路由权重的真源在 `recall.py:_ROUTE_WEIGHTS`。**两处名单各自维护，必然漂移**——新增路由后阈值校准会静默漏算该路由。

**为什么是问题**：调参需要改代码 + 重启 + 重新评测；阈值不可观测、不可 A/B；路由名单漂移导致校准失真且无任何报错。

**改进方案**：

- 把**检索质量相关**的常数（阈值、权重、倍率）集中到 `core/settings.py`，允许通过 `.env` 覆盖。运行时可调是调优的前提。
- 路由名单改为**从 `recall.py` 导出**（`ACTIVE_ROUTES = tuple(_ROUTE_WEIGHTS 的键集合)`），消除第二处真源。
- 为每类阈值加一个"最后校准时间 + 依据"注释，并在 §1 P0 第二步的质量回归测试里覆盖它们。

---

### P2 — 测试结构偏"装配验证"，缺"行为验证"

**证据**：47 个测试文件、486 用例，但 **233 处 `monkeypatch` / `MagicMock` / `patch`**。覆盖率高的是 `settings`、`factory`、`window` 这类**装配与纯函数**；链路级、行为级测试几乎空白。

**为什么是问题**：

- mock 过多时，测试验证的是"我调用了正确的函数"，而不是"系统产生了正确的结果"。**重构就红一片，真 bug 却照样漏过**（因为 mock 让被测对象永远按预期行为）。
- 检索链（recall → fusion → rerank → verifier）**没有任何端到端用例**，与 P0 互为因果。
- 有大量超时常量与 token 预算（`CONTEXT_TOKEN_BUDGET`、`_WINDOW_TOKEN_BUDGET`），但**没有一个性能/预算断言**。超时设计是否真的生效，无人验证。
- 前端 `static/app.js`（391 行）**零测试、零 lint**。

**改进方案**：

- **测试金字塔落地**：单元（纯函数，快，多）→ 集成（链路，用 fake 依赖，中）→ 端到端（真实 HTTP + fake 模型，少）。当前"倒金字塔"要摆正。
- **契约测试优先于 mock**：对 `aretrieve_evidence_with_retry` 这种对外入口，用 fake embedding/reranker 跑**真实链路**，断言返回结构（`RetrievalResult` 的 `status/docs/sources`），而不是断言"调用了 rerank"。
- **预算/超时断言**：至少加 3-5 个用例验证"超时确实中断""token 预算确实裁剪"。
- 前端加一个轻量冒烟：用 Playwright（项目已有 `playwright-cli` 能力）跑"登录 → 提问 → 看到回答"主路径。

---

### P2 — Chroma 索引落盘竞态：入库后立即查询会间歇性失败

**证据**（本轮建立检索门禁时实测）：向 ChromaDB `add_documents` 之后**立即**查询，
会间歇性抛：

```
Error executing plan: Internal error: Error creating hnsw segment reader: Nothing found on disk
```

**间歇性正是它危险的地方。** 同一份代码连续运行：

- 命中的集合**每次都不同**（实测在 `computer_organization` / `operating_system` 之间随机）。
- 命中后该集合**整轮不可查询**：该学科的检索结果全部来自别的学科，但不报任何错。
- 门禁指标会从 `category_hit@1` 0.900 掉到 0.725、`empty_result_rate` 从 0 升到 0.025 ——
  **看起来完全像一次严重的检索退化**，让人去改本来没坏的代码。

**本轮把根因查到了哪一步**（三条实测结论，都是"排除法"）：

| 检查项 | 结果 |
|---|---|
| `_ensure_hnsw_params` 是否在新建集合上调用 `modify()` 触发重建？ | **否** —— 打点确认 `needs_update=False`，`modify()` 从未执行。**排除** |
| 集合数据是否真的丢了？ | **否** —— `count()` 正确（如 419），HNSW 元数据 `status=ok` |
| 「丢弃 `_stores` 缓存句柄」能否恢复？ | **不能** |
| 「重开 `chromadb.PersistentClient`」能否恢复？ | **不能** |
| 「`delete_collection` + 重新入库」能否恢复？ | **能** |

**结论**：向量确实写进去了，但该集合的 **HNSW 段文件从未落盘**；进程内没有任何办法
把它"等"出来。这是 ChromaDB 写入侧的缺陷，**不是本项目可以靠重试绕过的**。

**已落地的处理（分层，而不是假装修好了）**：

1. **`VectorStoreManager.wait_until_ready`** —— 明确定位为**检测器而非修复器**。
   它把"静默返回错误检索结果"变成"入库期显式失败"，并在错误信息里直接给出补救命令
   （`ingest_category(<集合>, rebuild=True)`）。文档里写清了三种恢复手段的实测结果，
   避免后来者再去试"重试/重开 client"这两条死路。
2. **`rag/ingest.py`** —— 每个分类入库后做就绪检查；不可查询时记录 `not_ready` 并
   以 ERROR 级汇总（附补救命令），不再让故障无声通过。
3. **`rag/ingest.py` 的预热告警** —— `warmup_query_cache` 的成功率低于
   `settings.WARMUP_MIN_SUCCESS_RATE` 即判为异常，进日志（ERROR）与指标
   （`ingest_warmup_summary`）。此前只打印一行 INFO。
4. **门禁侧自动重建** —— `retrieval_gate.wait_for_index_ready(repair=True)`：
   门禁的索引是一次性的，重建无副作用，所以对不可查询的集合**只重建它自己**后重试。
   生产路径 `repair=False` —— 那里删集合意味着真实数据丢失，必须人工确认。

> **这一条同时说明了门禁的第一个真实价值**：它没抓出检索退化，却抓出了一个此前无人知道
> 的上游缺陷 —— 因为它是这个项目里第一个"**每次都在全新索引上跑完整链路**"的自动化流程。
> 既有的单测都在 mock 依赖，永远跑不到这条真实路径。这印证了 §1 P0 的判断：
> **测试真空的代价不是"没测试"，而是"没人知道代码实际在做什么"。**
>
> **未闭环的部分（明确记录）**：HNSW 段文件为何不落盘，尚未定位到 Chroma 内部原因，
> 因此**没有上游修复**。目前只有"检测 + 重建"这一层缓解。建议后续升级 ChromaDB 版本时
> 用 `evaluation.retrieval_gate` 复验是否消失 —— 它每次都会建全新索引，是最合适的复现器。

---

### P3 — 其它待办

- `src/tools/imputer.py` 1350 行（离线工具，优先级低，但若长期维护需拆分）。
- `_active_routes` 与 `_ROUTE_WEIGHTS` 的真源统一（见 P2）。
- `docs/` 在 `.gitignore` 中，导致工程文档无处存放；本报告放在根目录 `ENGINEERING.md` 以纳入版本控制。
- **仓库行尾不统一**（本轮新增发现）：`src/` + `tests/` 共 150 个 `.py` 文件中，
  **115 个是 CRLF、35 个是 LF**，且仓库**没有 `.gitattributes`**。同一目录内混用两种行尾
  （如 `tests/rag/` 下部分文件 CRLF、部分 LF）。风险不在功能，而在协作与工具链：
  同一行改动在不同平台上产生整文件 diff、评审噪声、以及脚本按 `\n` 匹配锚点时的静默失配。
  建议：新增 `.gitattributes` 统一 `* text=auto eol=lf`（Python 项目惯例），
  并在一次独立提交里完成行尾归一（**不要混在功能改动里**，否则 diff 不可读）。

---

### 本轮的方法论教训：日志与 stdout 交错会伪造「空结果」

记录在这里，因为它比任何单个缺陷都更容易重复踩到。

排查"数据结构类 query 是否返回空"时，我用
`python probe.py 2>&1 | grep -v "Async Chroma query failed"` 过滤日志噪声，
结果看到 6 条 `ds` query **完全没有命中行**，于是得出"`data_structure` 检索整体失效"的结论。

**这个结论是错的。** 真实原因是：logging 走 stderr、`print` 走 stdout，两者
**按行交错**；当一条证据行与一条日志行落在同一物理行上时，`grep -v` 会把整行一起删掉。
"没有输出"其实是"被我自己的过滤器删掉了"。

把结果改成 **JSON 落盘**后复测：40 条 query **零空结果**，`ds` 类全部命中 `data_structure`。

**由此得出两条硬性要求**（已同时写进 §3.1 评审要点）：

1. **凡是"统计/测量"脚本，结果一律走结构化输出（JSON/CSV）落盘，不要靠人眼读 stdout。**
   过滤日志噪声的操作，本质上是在不可靠的信道上做减法。
2. **看到"零结果 / 全失败"这类极端结论时，先怀疑测量工具，再怀疑被测系统。**
   极端结论应该先被复现一次（换个观测通道），再写进报告。

---

### 优先改进模块清单

| 优先级 | 模块 | 核心问题 | 建议动作 | 预期收益 |
|---|---|---|---|---|
| **P0** | `rag/retriever.py` | 1872 行 / 7% 覆盖 / 447+427 行双胞胎函数 | 拆阶段函数 + 纯函数单测 + 去重同步版 | 可测、可改、可调参 |
| **P0** | `evals/` + 检索质量 | 无自动化质量回归 | 黄金集进 CI 作门禁 | 防质量退化 |
| | ↳ 进度 | `retrieval_gate` + 42 用例 + 基线 + CI job `retrieval-quality-gate` | ✅ 已完成 | 基线 `hit@1` 0.900 / `hit@k` 1.000 |
| **P0** | `rag/{splitter,vectorstore,semantic_cache,embeddings}` | 0% 覆盖 | fake 依赖 + 集成测试 | 覆盖盲区 |
| | ↳ 进度 | `vectorstore` 0→35%、`semantic_cache` 0→57%、`embeddings` 0→30%、**`splitter` 0→99%** | ✅ 已完成 | 全项目 36%→48% |
| **P1** | `rag/splitter.py` Q&A 链路 | `merged_qa` 在真实语料**从未激活**，连带 `recall.merged_qa_meta` 路由恒空 | 对齐标注 + 修 `_merge_qa_blockquotes` + 决定路由去留 | 结构化问答检索可用 |
| **P1** | `rag/ingest.py` | 仍是 0% 覆盖 | 同 fake 依赖模式补测 | 入库链路可验证 |
| **P1** | 全项目异常处理 | 92 catch-all / 13 silent pass | 分层异常 + 禁 pass | 可观测性 |
| **P1** | `rag/**` 类型检查 | 9 类错误关闭 | 渐进开启，先 `unbound-name` | 早发现崩溃 |
| **P2** | 检索阈值与路由名单 | 魔数散落 / 名单双真源 | 集中到 settings / 单一真源 | 可调优 |
| **P2** | 测试结构 | 偏 mock / 无端到端 / 无性能断言 | 金字塔 + 契约测试 | 测试可信 |
| **P3** | `tools/imputer.py`、前端 | 超长文件 / 零测试 | 按需拆分 + 冒烟测试 | 长期可维护 |

---

## 2. 团队能力短板与最佳实践规范

### 2.1 短板诊断（从代码痕迹反推，非主观评价）

代码是团队思维方式的化石。从本项目痕迹看，四个可复用的能力缺口：

**短板一：会写"能跑的复杂逻辑"，不会写"可验证的复杂逻辑"**

`retrieve_documents` 447 行、`aretrieve_documents` 427 行、`split_documents` 230 行——能写出来说明**业务理解和技术能力都不差**。但覆盖率 7%/0% 说明：面对"异步 + 多路并发 + 外部依赖"的复杂场景，团队**不知道从哪下刀写测试**，于是选择不写。

> 根因：缺乏"**先设计可测边界，再实现逻辑**"的习惯。测试写不出来，往往是因为函数边界本身没设计好——447 行的函数无法测，不是测试的问题，是设计的问题。

**短板二：抽象与分解的粒度感不足**

团队会做**横向分层**（agents/rag/core 分得很清楚），但**纵向分解**不足——同一个模块内部动辄 400+ 行函数。这说明抽象停在"模块级"，没下探到"函数级职责单一"。

**短板三：异常处理策略是"兜底"而非"设计"**

92 处 `except Exception` 反映一种心态：**怕崩，所以全都接住**。但"接住"不等于"处理"。真正的工程判断是：哪些错误该重试、哪些该降级、哪些该让用户看到、哪些必须让系统崩掉（fail-fast）。项目里其实**已经有正确答案的样本**——`supervisor.py` 的 `UnclosedSpecialistOutput` 就是优秀的 fail-fast 设计。**把这种思路推广到全项目**，就是提升点。

**短板四：把类型系统当负担，而不是当工具**

用"整目录关闭检查"解决类型报错，而不是"修好类型或精确豁免"。这与 `settings.py` 里认真写 `Annotated[str, BeforeValidator(...)]` 形成鲜明对比——**团队里有懂类型的人，但没形成共识和规范**。

**短板五：性能与成本意识有设计、无度量**

项目里有丰富的超时常量、token 预算、多级缓存——设计意识是好的。但**没有任何性能测试、没有 token 用量统计、没有缓存命中率的观测面板**（`_cache_stats_fields` 收集了数据却没人消费）。**做了防护，但不知道防护是否生效**。

### 2.2 最佳实践规范（团队工程宪法）

以下规范**全部针对上述短板**，可直接执行。

#### 规范 1：函数与模块规模红线

| 对象 | 上限 | 超出处理 |
|---|---|---|
| 单函数 | 60 行 | 拆分为阶段函数；确需超出须在 PR 说明理由 |
| 单文件 | 600 行 | 按职责拆模块 |
| 单函数参数 | 5 个 | 改用 dataclass / Pydantic 模型传参 |
| 嵌套深度 | 4 层 | 提取早返回（early return） |

**为什么是 60 行**：一个函数超过一屏，评审者无法在脑中完整推演其所有分支——**不可评审的代码等于没有评审**。

#### 规范 2：分层异常处理

```python
# 1) 定义域内异常层级（不要用字符串传递错误语义）
class RagError(Exception): ...
class RetrievalUnavailable(RagError): ...   # 依赖不可用 → 可重试/可降级
class RetrievalEmpty(RagError): ...         # 正常空结果 → 不记 ERROR
class RagInternalError(RagError): ...       # 代码缺陷 → 必须告警

# 2) 禁止裸 pass
except SomeError:
    logger.debug("...", exc_info=True)   # 必须留痕
    # 注释说明为何此处忽略是安全的

# 3) catch-all 只允许在最外层边界，且必须分级
try:
    ...
except (RetrievalUnavailable, TimeoutError) as e:   # 预期内失败 → 降级 + WARNING
    return _degraded_result(e)
except Exception:                                    # 非预期 → ERROR + 上报
    logger.exception("unexpected failure in retrieval")
    raise

# 4) fail-fast 优先于兜底
#    数据契约被破坏时（如专家输出未闭环），宁可崩也不要静默降级
#    参照 agents/supervisor.py: UnclosedSpecialistOutput
```

#### 规范 3：可测试性作为设计约束（而非事后补救）

- **纯逻辑与副作用分离**：计算逻辑写成纯函数（输入 → 输出），I/O 与外部调用放在薄壳层。本项目 `_apply_rerank_threshold`、`weighted_rrf_merge` 就是正面样本。
- **依赖注入**：外部依赖（LLM / embedding / 存储）必须可替换。项目已有 `USE_FAKE_MODEL`、`get_store()` 注入模式，**新代码必须遵循**。
- **每个新增/修改的函数，PR 必须附带其测试**。无法测试的代码，说明边界设计有问题，先重构再提交。
- **禁止"为了测试而 mock 掉被测逻辑本身"**：mock 只用于外部依赖，不用于被测对象。

#### 规范 4：配置外置与单一真源

- 所有**可调参数**（阈值、权重、倍率、预算）进 `core/settings.py`，`.env` 可覆盖；代码中只允许存在"算法内生的数学常量"（如 `RRF k=60`）。
- **同一份数据只允许一个真源**。路由名单、模型清单、prompt 版本等，禁止在第二个地方复制列表（现有 `_active_routes` vs `_ROUTE_WEIGHTS` 就是反例）。
- 新增配置项**必须同步 `.env.example`**（已有约定，继续执行）。

#### 规范 5：可观测性

- 日志分级要有语义：`ERROR` 只用于"需要人介入"的事件；预期内的降级用 `WARNING`；正常空结果用 `INFO`。
- **关键路径必须可追踪**：检索链每一步（召回数量、融合分数、rerank 分数、是否命中缓存）已有 INFO 日志——保持，并补齐指标消费（把 `_cache_stats_fields` 接到 `/health` 或 `/metrics`）。
- 每个 catch-all 必须能回答："出问题时，日志能不能让我定位到具体环节？"

#### 规范 6：异步与并发

- 同步阻塞调用**禁止出现在 async 函数中**，必须 `asyncio.to_thread` 包装（项目已有 `_safe_to_thread` 工具，继续用）。
- 所有外部调用**必须有超时**；所有并发分支**必须有失败隔离**（`as_completed` + 单分支异常不拖垮整体，`_multi_route_search` 已是正面样本）。
- 共享可变状态（缓存、全局单例）必须线程安全且**有界**（`BoundedCache` 是标准做法）。

#### 规范 7：安全

- 密钥一律 `SecretStr`，取值 `.get_secret_value()`，**禁止出现在日志/异常消息里**。
- 所有来自客户端、可能访问他人数据的标识（如 `thread_id`）**必须校验归属**（`_check_thread_owner` 是标准做法）。
- 外部输入一律经 Pydantic 校验；前端输出必须转义（`static/app.js` 需明确 XSS 转义策略）。

---

## 3. 代码质量把控标准

### 3.1 代码评审要点（Reviewer Checklist）

**评审者的职责是"把关"而非"挑刺"。按以下顺序检查，前项不过不进入后项：**

**A. 正确性（Blocking）**
- [ ] 边界条件：空输入、空结果、超长输入、None 是否处理？
- [ ] 并发安全：共享状态是否加锁/有界？异步中是否有阻塞调用？
- [ ] 错误路径：异常是否被正确处理（分层、留痕、不吞）？失败时状态是否一致？
- [ ] 外部依赖：是否都有超时？失败是否隔离（不拖垮主流程）？
- [ ] 契约变更：对外接口（`schema/`）改动是否向后兼容？改了是否同步调用方与文档？

**B. 可测性（Blocking）**
- [ ] 新增/修改逻辑是否有对应测试？测试是验证**行为**还是只验证**调用**？
- [ ] 是否引入了难以测试的设计（长函数、硬编码依赖、隐藏状态）？
- [ ] 覆盖率是否覆盖到本次改动的分支（而非整体数字）？

**C. 可维护性（Blocking）**
- [ ] 函数是否 ≤ 60 行、文件 ≤ 600 行？超限是否有正当理由？
- [ ] 命名是否表达意图（`data`/`temp`/`result2` 一律打回）？
- [ ] 是否有解释"为什么"的注释（而非复述代码的"是什么"）？
- [ ] 是否违反单一真源（复制了列表/常量/逻辑）？

**D. 性能与成本（重要）**
- [ ] 是否引入了额外的 LLM 调用 / 检索调用？能否复用缓存？
- [ ] token 预算是否受控（长文本拼接是否有上限）？
- [ ] 是否有 N+1 式的串行外部调用（应并发）？

**E. 安全（Blocking）**
- [ ] 密钥/敏感信息是否泄漏到日志或响应？
- [ ] 客户端可控的标识是否做了归属/权限校验？
- [ ] 输入是否经校验？输出是否转义？

**F. 一致性（建议）**
- [ ] 是否遵循 CLAUDE.md 的既有约定（配置走 settings、LLM 走工厂、注册 agent 同步三处）？
- [ ] 类型注解是否完整（新增代码不得依赖目录级放宽）？

**G. 验证方法（Blocking，本轮新增 —— 全部来自真实踩过的坑）**
- [ ] **"没报错"是否被当成了"环境是封闭的"？** 评测/门禁类改动的验收标准必须包含
      "它依赖哪些外部资源"，并在**无外部资源的条件下**跑一遍。
      （本轮实测：`USE_FAKE_MODEL=true` 下检索链仍调用**真实 LLM**，
      本地每次结果不同、CI 直接崩 6/40 条 —— 全程无任何报错。）
- [ ] **门禁自身的噪声是否测量过？** 连续跑 N 次确认可复现，再定容差。
      若噪声 ≥ 容差，那不是门禁，是随机数生成器。
      （本轮实测：`hit@1` 噪声 0.025 > 容差 0.02，根因是 RRF 并列排序依赖线程完成次序。）
- [ ] **"连续 N 次一致"是否可能只是巧合？** 本轮把连续 4 次一致当成了"确定性已修复"的证据，
      但当时基线跑在真实 LLM 上 —— 那是巧合。**要说明为什么它应该一致，而不只是观察到它一致。**
- [ ] **测试断言是否"恒真"？** 构造输入时要确认它真的能触发被测分支。
      反例：让两个文档各占一条同权重同 rank 的路由 → 分数并列，
      `scores == sorted(scores, reverse=True)` 恒真，测不出排序方向。
      **反向验证是唯一可靠的检出手段。**
- [ ] **缺陷的严重性是否用真实数据量过？** 合成用例能证明"存在这条路径"，
      证明不了"这条路径有多重要"。（本轮实测：全库留存率 97.6%，
      362 个"丢失" section 里 318 个是只有标题的空 section。）
- [ ] **"修复"是否被端到端指标验证过？** 单测全绿 + 审查通过 ≠ 端到端没退化。
      涉及数据/检索的改动必须过端到端指标。
- [ ] **测量脚本结果是否走结构化输出落盘？** 不要靠人眼读 stdout，更不要用 `grep -v`
      从混合日志里"减"出结论 —— logging 走 stderr、print 走 stdout 按行交错，
      会把证据行一起删掉，制造出"检索返回空"的假象。
- [ ] **注入/恢复脚本是否用原始字节备份？** 若备份取自"已规范化换行"的文本，
      恢复时会把 CRLF 改成 LF，而"两边都规范化后再比较"的校验**必然通过**。
- [ ] **新增门禁/断言前，是否测量过它自身的噪声？** 连续跑 N 次确认结果可复现，
      再决定容差。**若噪声幅度 ≥ 容差，那不是门禁，是随机数生成器。**
      （本轮实测：检索门禁的 `hit@1` 噪声约 0.025 > 容差 0.02，
      根因是 RRF 并列排序依赖线程完成顺序 —— 详见 §1 P1。修掉不确定性后噪声归零。）
- [ ] **测试断言是否"恒真"？** 构造输入时要确认它真的能触发被测分支。
      本轮两处反例：让两个文档各占一条同权重、同 rank 的路由 → 分数并列，
      于是 `scores == sorted(scores, reverse=True)` 恒真，测不出排序方向；
      以及用 2 个元素验证"按 key 升序"的约定 —— 任何确定性次级键都可能碰巧给出同序。
      **反向验证是唯一可靠的检出手段。**
- [ ] **缺陷的严重性是否用真实数据量过？** 合成用例能证明"存在这条路径"，
      证明不了"这条路径有多重要"。本轮教训：我用手写的短例题推出"严重内容丢失"，
      实测全库留存率 97.6%，且 362 个"丢失" section 里 318 个是**只有标题的空 section**
      —— 过滤它们是正确行为。**先量分布，再定优先级。**
- [ ] **"修复"是否被端到端指标验证过？** 本轮我修了一个逻辑自洽、三个新测试全绿的
      "原子单元被合并"问题，门禁显示留存率 97.6%→96.5%、精确率 0.9014→0.8952
      —— 已回退。**单测全绿 + 审查通过 ≠ 端到端没退化**，涉及数据/检索的改动必须过端到端指标。

> **评审节奏建议**：单次 PR ≤ 400 行改动。超过则要求拆分——**评审质量与改动规模成反比**。

### 3.2 编码规范（项目特定）

**已有约定（继续执行，来自 CLAUDE.md）**：
- 配置走 `core.settings.settings` 单例；LLM 走 `get_model` / `get_llm` 工厂；模型标识 `gateway:model_id`，不按名字猜网关。
- 注册 agent 同步 `agents.py` 导入 + `agents` 字典 + `langgraph.json` 三处。
- 关键逻辑与复杂函数写中文注释，**解释设计意图与权衡**。

**新增规范**：
1. **类型注解强制**：新增代码必须完整注解，`pyrefly` 不得新增豁免；存量放宽逐项收敛。
2. **异常规范**：见 §2.2 规范 2。
3. **`__future__ import annotations`**：异步/复杂模块统一使用（项目已部分采用，保持一致）。
4. **导入顺序**：由 ruff 的 `I` 规则保证（已配置），保持。
5. **文档字符串**：公开函数必须有 docstring，说明**职责、参数语义、失败行为**（项目现状良好，保持并补齐缺失项）。
6. **禁止新增全局可变状态**：除有界缓存与配置单例外，模块级变量必须为常量。
7. **日志**：使用模块级 `logger = logging.getLogger(__name__)`，禁止 `print`。

### 3.3 测试要求

**测试金字塔（目标比例）**：

| 层级 | 占比 | 特征 | 本项目现状 |
|---|---|---|---|
| 单元测试 | ~60% | 纯函数、快（<1s）、无外部依赖 | 尚可 |
| 集成测试 | ~30% | 链路级，用 fake 依赖 | **严重不足** |
| 端到端 | ~10% | 真实 HTTP + fake 模型 | 部分（service 层有） |

**必须测试的场景（硬性要求）**：
1. **每个公开函数**至少一个正常路径用例。
2. **每个错误分支**至少一个用例（超时、空结果、外部失败、非法输入）。
3. **所有阈值/预算逻辑**必须有边界用例（恰好等于阈值、低于、高于）。
4. **对外契约**（`schema/`、API 响应）必须有结构断言测试。
5. **并发/异步路径**至少一个用例（含"单分支失败不影响整体"）。

**覆盖率门槛（分模块差异化——这是关键改进）**：

```yaml
# codecov.yml 建议改造：把"整体 36%"变成"按风险分层设门槛"
coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
  # 关键：对高风险模块单独设门槛，避免被高覆盖模块稀释
  # （Codecov 通过 flags/components 实现，或改用 pytest 的 --cov-fail-under 分模块校验）
```

| 模块 | 目标覆盖率 | 理由 |
|---|---|---|
| `src/rag/**` | **≥ 70%** | 核心链路，当前 7-39%，最高风险 |
| `src/agents/**` | ≥ 85% | 业务核心，当前已 91%+ |
| `src/service/**` | ≥ 85% | 对外契约，当前 82% |
| `src/memory/**` | ≥ 85% | 数据正确性，当前 86%+ |
| `src/core/**` | ≥ 90% | 基础设施 |
| `src/tools/**` | 不设门槛 | 离线工具（现状合理） |

**质量回归门禁（新增，最高价值）**：
- `evals/` 黄金集进 CI，断言 `recall@k` / `MRR` / RAGAS 分数不低于基线。
- **这是防止"代码没 bug 但效果变差"的唯一手段**，优先级高于覆盖率数字。

### 3.4 CI 门禁增强建议

现状（`.github/workflows/test.yml`）已覆盖 lint / format / type / test / cov / docker / md lint，很完整。建议增补：

1. **分模块覆盖率门槛**：`pytest --cov=src/rag --cov-fail-under=70` 等，避免整体数字掩盖风险。
2. **改动覆盖率（patch coverage）从 informational 改为阻塞**：`codecov.yml` 目前 `patch.informational: true`，意味着**新代码可以零测试合入**。改为 `informational: false` + `target: 80%`——这是"新债不再产生"的关键闸门。
3. **复杂度门禁**：接入 `ruff` 的 `C90`（mccabe）规则或 `radon`，对超阈值函数报警。
4. **检索质量回归**：✅ **已落地**（本轮）——新增独立 job `retrieval-quality-gate`，
   跑 `uv run python -m evaluation.retrieval_gate`，不依赖任何外部服务，耗时约 35s。
   与覆盖率门禁互补：覆盖率保证"代码被跑过"，它保证"跑出来的结果没变差"。
5. **依赖安全扫描**：`uv.lock` 已有锁定，建议加 `pip-audit` 或 Dependabot。

> **门禁设计原则**：门禁应当**阻止新增债务**，而不是**逼人还清旧债**。旧债用计划逐步还（见 §4），新债一律不许进。

> **门禁的第一个真实收益（本轮实测）**：检索门禁上线后，它**没有**抓到检索退化，
> 却抓出了一个此前无人知道的环境竞态（Chroma HNSW 落盘未完成即查询 → 间歇性失败，
> 详见 §1 P2）。原因是它是这个项目里第一个"**每次都在全新索引上跑完整链路**"的自动化流程 ——
> 既有的单测都在 mock 依赖，永远跑不到这条真实路径。
> 这印证了 §1 P0 的判断：**测试真空的代价不是"没测试"，而是"没人知道代码实际在做什么"。**

---

## 4. 技术能力提升路径（初级 → 资深）

### 4.1 能力模型

| 级别 | 核心特征 | 在本项目的可验证标志 |
|---|---|---|
| **L1 可交付** | 能在指导下完成明确任务 | 能按规范新增一个 tool / 一个 API 端点，含测试 |
| **L2 可独立** | 独立完成模块，代码可评审通过 | 能独立实现一个 RAG 子模块（如新增一路召回），测试齐全，无 reviewer 打回 |
| **L3 可设计** | 能设计模块边界与接口，预判失败模式 | 能主导拆分 `retrieve_documents`，产出可测的阶段划分并说明权衡 |
| **L4 可负责** | 对系统质量负责，能定标准、带人 | 能制定本项目的检索质量门禁，能 review 他人设计并给出可执行改进 |

**判断资深的不是"写了多少代码"，而是"能否在信息不全时做出可解释的权衡，并为结果负责"。**

### 4.2 四阶段提升路线图

**阶段一：把"能跑"变成"可验证"（4-6 周）**

- **目标**：建立"先想测试再写实现"的条件反射。
- **配套任务**（直接用项目真实 backlog，边做边学）：
  1. 为 `retriever.py` 的**纯函数**补单测（`_apply_rerank_threshold` / `_resolve_retrieval_policy` / `_route_adaptive_k` / `_route_timeout_seconds`）。→ 练"识别可测边界"。
  2. 为 `memory/window.py`、`fusion.py` 补边界用例。→ 练"穷举分支"。
  3. 建立 `USE_FAKE_EMBEDDING`，让 `vectorstore` / `semantic_cache` 可测。→ 练"依赖注入与测试替身"。
- **学习重点**：pytest fixture 设计、参数化测试、`monkeypatch` 的正确用法（只替外部依赖）、异步测试。
- **验收**：`src/rag/**` 覆盖率从 <40% 提升到 ≥60%；新代码 patch coverage ≥80%。

**阶段二：把"长函数"变成"好结构"（6-8 周）**

- **目标**：建立函数级抽象能力与"可读性即正确性"的信念。
- **配套任务**：
  1. 拆分 `retrieve_documents` / `aretrieve_documents` 为阶段函数，**消除同步/异步双份逻辑**（这是最好的抽象练习：已有清晰注释告诉你边界在哪）。
  2. 统一路由名单真源（`_active_routes` ← `_ROUTE_WEIGHTS`）。
  3. 把检索阈值集中到 settings。
- **学习重点**：重构的安全操作方式（小步提交 + 测试护航）、纯函数与副作用分离、圈复杂度控制。
- **验收**：检索链单函数 ≤60 行；重构前后质量回归测试全绿（证明行为不变）。

**阶段三：把"兜底"变成"设计"（4-6 周）**

- **目标**：建立失败模式思维——**主动设计系统如何失败**。
- **配套任务**：
  1. 引入分层异常（`RagError` 层级），替换字符串化错误。
  2. 清理 13 处 `except: pass`，把 92 处 catch-all 收敛到合理边界。
  3. 补齐关键路径的超时/降级测试。
- **学习重点**：fail-fast vs fail-safe 的判断标准、错误分类、可观测性设计（日志分级 + 指标）。
- **验收**：`grep -c "except: pass"` 归零；每个 catch-all 能回答"能否定位问题"。

**阶段四：把"改代码"变成"定标准"（持续）**

- **目标**：从"解决眼前问题"升级为"让问题不再发生"。
- **配套任务**：
  1. 主导建立**检索质量回归门禁**（黄金集 + CI）。
  2. 收敛 `rag/**` 的类型豁免，逐类开启。
  3. 主导一次架构评审或故障复盘，产出可执行改进项。
- **学习重点**：技术决策的权衡表达（写 ADR）、跨模块影响分析、带人（review 他人代码时给"为什么"而非"怎么改"）。
- **验收**：能独立产出一份被采纳的工程标准或架构决策文档。

### 4.3 配套机制（比路线图更重要）

技术能力不会因为"看了文档"而提升，只会因为"**在真实约束下反复做判断并被反馈**"而提升。

1. **结对评审（每周 1 次，30 分钟）**：随机抽一份本周 PR，两人对读，重点讲"**为什么这么设计**"而非"语法对不对"。资深者要示范"如何提出建设性意见"。
2. **故障复盘（每次线上问题）**：不问"谁写错了"，只问"**为什么这个错误能被写出来且没被测出来**"。产出必须落到"门禁/测试/规范"的改进，而非"下次注意"。
3. **技术分享（每两周 1 人）**：讲本项目的真实难题——比如"多路召回为什么用 RRF 而不是加权求和""同步/异步双份实现如何统一"。**讲自己的代码是最好的学习**。
4. **轮值 owner**：每周一人负责"质量门禁"——检查新 PR 的 patch coverage、是否有新增 `except: pass`、函数是否超限。**让每个人都当过"守门人"**，规范才会内化。
5. **读优秀代码**：把本项目 `supervisor.py`（fail-fast 设计）、`core/llm.py`（缓存与状态陷阱）、`core/cache.py`（有界缓存 + 淘汰回调）当作**内部教材**——团队里已经有人能写出好代码，**让其他人读懂它，比看外部文章更有效**。

### 4.4 一句话总结

> 这个团队**不缺技术能力，缺的是质量纪律的覆盖面**。
> 提升的关键不是"学更多技术"，而是把已有的好习惯（清晰的注释、防御性设计、fail-fast）**从"某些模块"推广到"所有模块"**，尤其是那个承载全部产品价值、却最缺乏保护的检索链。

---

## 附录 A：可直接进 Issue 的改进 Backlog

「状态」列里的 ✅ 表示本轮已落地并复测；其余为待办。验收标准保留原始目标，
**已完成的会额外标注实测值**，便于对照"目标 vs 实际"。

| # | 任务 | 优先级 | 验收标准 | 状态 |
|---|---|---|---|---|
| 1 | 为 `retriever.py` 纯函数补单测 | P0 | 覆盖率 ≥30% | ✅ 实测 **7%→18%**（未达 30%，原因见 §1 P0 第一步：纯函数有结构性上限，须与 #4 并行） |
| 2 | 建立 `USE_FAKE_EMBEDDING` 与集成测试 | P0 | 三个模块 ≥60% | ✅ 实测 `semantic_cache` **0→57%**、`vectorstore` **0→35%**、`embeddings` **0→30%** |
| 3 | 为 `splitter.py` 补单测 | P0 | 覆盖率 ≥60% | ✅ 实测 **0→99%**（218 用例，反向验证 18/18） |
| 4 | 检索质量黄金集进 CI | P0 | 30+ query，`recall@5` 不低于基线，CI 阻塞 | ✅ 实测 40 query；`category_hit@1` **0.900**、`hit@k` **1.000**、`MRR` **0.946**；CI job 已阻塞 |
| 5 | 拆分 `retrieve_documents`/`aretrieve_documents` | P1 | 单函数 ≤60 行，双份逻辑合并，回归全绿 | 🟡 **双份逻辑已合并**（同步版 447→45 行薄委托，删 194 语句，`retriever.py` 覆盖率 18%→25%）；**拆 428 行的异步单体函数仍待做** |
| 6 | **修复 `splitter` Q&A 链路**：对齐 `_ANSWER_RE` 标注 | P1 | 真题文件产出 `merged_qa` chunk > 0 | ⬜ 需先做 #4 |
| 7 | **修复 `_merge_qa_blockquotes` 的原子块切碎问题** | P1 | 含空行的 Q&A 块仍为单一 `is_qa` 单元 | ⬜ #6 的前置 |
| 8 | **决策 `recall.merged_qa_meta` 路由去留** | P1 | 要么有数据、要么删除路由与 RRF 权重 | ⬜ 与 #6 联动 |
| 9 | 修复 `split_documents` 的 `chunk_size`/`chunk_overlap` 失效 | P1 | 传入参数生效，或删除参数避免误导 | ⬜ 有变更哨兵测试 |
| 10 | 修复短例题 section 产出 0 chunk（内容静默丢失） | P1 | 短 Q&A section 至少产出 1 个 chunk | ⬜ |
| 11 | 引入分层异常，清理 `except: pass` | P1 | `except: pass` 归零，新增域异常层级 | ⬜ |
| 12 | 收敛 `rag/**` 类型豁免 | P1 | 先开 `unbound-name`/`missing-import` | 🟡 真实错误已修（`supervisor.py`），豁免仍待收敛 |
| 13 | 支持 5 级标题（`##### 1)内容`） | P2 | 真实库 80 处五级标题进入 section 层级 | ⬜ |
| 14 | 检索阈值集中到 settings + 路由名单单一真源 | P2 | 阈值可经 `.env` 覆盖；名单从 `recall.py` 导出 | ⬜ |
| 15 | 补 `rag/ingest.py` 测试（唯一剩余 0% 模块） | P2 | 覆盖率 ≥50% | ✅ 实测 **0→84%**（`tests/rag/test_ingest_readiness.py`，28 用例，反向验证 14/14） |
| 16 | codecov patch coverage 改为阻塞 | P2 | 新代码 patch coverage ≥80% 才可合入 | ⬜ |
| 17 | 分模块覆盖率门槛 | P2 | CI 按 `rag/agents/service` 分别校验 | ⬜ |
| 18 | 加 `.gitattributes` 统一行尾 | P2 | 消除 CRLF/LF 混用（**须独立提交**，勿混功能改动） | ⬜ |
| 19 | 前端冒烟测试 | P3 | Playwright 跑通"登录→提问→回答" | ⬜ |
| 20 | 缓存指标接入可观测端点 | P3 | `/health` 或 `/metrics` 暴露缓存命中率 | ⬜ |
| 21 | **入库后就绪屏障**（Chroma HNSW 落盘竞态） | P2 | `ingest` 后查询不再间歇失败；参考实现见 `retrieval_gate.wait_for_index_ready` | 🟡 检测+重建已落地；**上游根因未定位** |
| 22 | `warmup_query_cache` 失败率接入日志/告警 | P2 | 预热成功率低于阈值时告警，而非只打印 | ✅ 已落地（ERROR + `ingest_warmup_summary` 指标 + 阈值走 settings） |
| 23 | 检索结果不可复现（RRF 并列排序依赖线程完成顺序） | P1 | 同一 query 多次运行结果逐位一致 | ✅ 已落地（确定性次级键，反向验证 4/4） |
| 24 | `_split_oversized` 单句超限无兜底 | P3 | 任意输入下每段 ≤ limit | ✅ 已落地（反向验证 3/3；真实语料不触发） |
| 25 | 短 section 静默丢失（`_flush_chunk` 摘尾 + `MIN_CHUNK_LENGTH`） | P3 | 全库留存率 > 99% | ⬜ 实测 97.6%，其中 318/362 是空标题 section（非缺陷），真实损失约 2.4% |
| 26 | `splitter` docstring 与实现不符（"原子单元不可合并"） | P3 | 注释与行为一致 | ✅ 已修正 docstring + 用测试钉住实测数字 |
| 27 | 清理因合并双份逻辑而变成孤儿的 `decompose_sync` | P3 | 删除或明确保留理由 | ⬜ 已无仓库内调用方；删除属对外 API 变更，交由负责人决定（`call_structured_sync` 需一并评估） |
| 28 | 拆分 `aretrieve_documents`（428 行单体函数） | P1 | 拆成 ≤6 个阶段函数，每个可单测 | 🟡 **已拆 6/9 段**：归一化分类 / 策略解析 / 分解 / 去重+阈值 / 召回融合 / **重排**（428→**309 行**，累计 -119），新增 35 条阶段测试、反向验证 25/25；阶段级比对 29 次 28 通过（唯一失败是已知 ANN query），门禁逐位一致。剩 3 段（HyDE / 窗口展开 / 收尾） |
| 29 | recall 阶段残余非确定性 | P1→**P2** | 同一 query 反复运行阶段级一致 | 🟡 已定位到 Chroma ANN 边界；**但已证实会传导到最终结果**（约 1/10 次），产品侧确有低频不可复现。已有 `--stable-only` 规避对验证的干扰，但**产品行为本身仍未修复**（上游根因，需升级 Chroma 或加大 ANN 候选池） |
| 30 | `get_llm` 与 `get_model` 的 fake 分支不对称 | P1 | 两个入口行为一致 | ✅ 已落地（反向验证含在 15 条封闭性测试里） |
| 31 | `bm25_search` 三处理论隐患（`get(limit=N)` 无序截断 / `limit=k*3` 无序候选集 / 最终 sort 无并列次级键） | P3 | 加固为确定性实现 | ⬜ 已实测**不是** #29 的原因，但仍是隐患，建议顺手加固 |
| 32 | 阶段追踪器改**分级断言**（recall 层只报警，其余严格） | P2 | 可在 ANN 边界抖动下稳定判定重构无损 | ✅ 已落地（严格 6 次连跑全通过，反向验证 5/5）；#28 已解锁 |
| 33 | 重排失败兜底在分解路径少留一半候选（`default=filtered[:k]` 但候选池是 `k*2`） | P3 | 两条路径兜底口径一致 | ⬜ 拆分时发现的既有不对称；本次只搬代码未改，已在代码里留注释 |

## 附录 B：评审时最该问的 6 个问题

1. **这个改动失败时，系统会怎样？**（失败模式是否被设计过）
2. **这个逻辑怎么测？测不了是不是因为边界没划好？**
3. **有没有第二份真源？**（复制了列表/常量/逻辑）
4. **这里多花了一次 LLM 调用吗？缓存能用吗？**
5. **半年后的人读这段代码，能明白为什么这么写吗？**
6. **这个分支/字段/路由，最近一次真正执行是什么时候？**
   （本轮 `merged_qa` 死链路就是被这一问挖出来的：一个特性、一条检索路由、
   三个元数据字段，全部"看起来在工作"却从未被执行过。**测试覆盖率能回答这个问题，
   而代码审查不能** —— 这也是为什么 §1 把测试真空列为 P0。）
